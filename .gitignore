# Rust
/target/
**/*.rs.bk

# Tauri build artifacts
/crates/strand-tauri/gen/
/crates/strand-tauri/target/
/crates/strand-tauri/binaries/*
!/crates/strand-tauri/binaries/.gitkeep

# Node / Vite
node_modules/
/.pnpm-store/
ui/dist/
ui/.vite/
**/*.tsbuildinfo
ui/src/**/*.js
/website/dist/
/website/demo/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
# Atomic-write temp files (editors/agents write `<name>.tmp.<pid>.<hash>` then
# rename over the target; orphans linger if the rename races a lock on Windows).
*.tmp.*

# Local env
.env
.env.local

# Bundled design handoff (kept on disk for reference, not in git)
/Strand_extracted/
/Strand.zip
.playwright-mcp
/.superdesign/tmp/

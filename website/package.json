{
  "name": "strand-website",
  "private": true,
  "version": "0.0.1",
  "description": "Static landing page and pre-rendered user guide for strandgit.com.",
  "scripts": {
    "build": "node build.mjs",
    "check:seo": "node seo-check.mjs",
    "test": "npm run build && npm run check:seo",
    "start": "serve dist -l ${PORT:-4321}",
    "serve": "npm run build && serve dist -l 4321"
  },
  "dependencies": {
    "serve": "^14"
  }
}

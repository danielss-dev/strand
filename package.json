{
  "name": "strand",
  "private": true,
  "version": "1.0.0",
  "description": "Fast, friendly Git client (Tauri 2 + Rust + React).",
  "scripts": {
    "dev": "pnpm --filter strand-ui dev",
    "build": "pnpm --filter strand-ui build",
    "clean:js": "pnpm --filter strand-ui clean:js",
    "site": "pnpm --filter strand-website serve",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "release:check-security": "node scripts/check-release-security.mjs",
    "release:check-updater-signatures": "node scripts/check-updater-signatures.mjs",
    "version:set": "node scripts/bump-version.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "packageManager": "pnpm@9.0.0",
  "pnpm": {
    "patchedDependencies": {
      "@pierre/trees@1.0.0-beta.5": "patches/@pierre__trees@1.0.0-beta.5.patch"
    }
  }
}
